[package]
name = "lib"
version = "0.1.0"
edition = "2021"

[dependencies]
node-bindgen = { version = "6.0.1" }

[build-dependencies]
node-bindgen = { version = "6.0.1",  default-features = false, features = ["build"] }

[lib]
crate-type = ["cdylib"]